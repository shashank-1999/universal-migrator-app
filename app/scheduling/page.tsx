export default function SchedulingPage() {
  return (
    <main>
      <h1 style={{fontSize:22, fontWeight:700, marginBottom:12}}>Scheduling</h1>
      <p>Scheduler storage and cron runner will be wired after initial migrations. For now, create runs from the Workflow page.</p>
    </main>
  );
}